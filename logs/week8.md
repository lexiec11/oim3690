## session 14
## what i learned
- I learned how to embed external content like YouTube videos and Google Maps using `iframe`.
- I learned how to add media to websites using the `audio` and `video` tags.
- I learned how to create collapsible sections using `details` and `summary`.
- I also learned how the `canvas` element can be used to draw graphics on a webpage.

## what i did
- We also had a **class quiz on Session 14**.
- I created a `playground.html` page in my repository to experiment with different HTML features.
- I embedded a YouTube video and a Google Map.
- I added audio and video elements with controls.
- I created a collapsible FAQ using `details` and `summary`.
- I used `canvas` to draw simple shapes with JavaScript.




## session 15
## what i learned
- I learned how JavaScript can make webpages interactive by responding to user actions.
- I learned how to select elements on a page using `querySelector()` and `getElementById()`.
- I learned how `addEventListener()` works to run functions when events like clicks happen.
- I also learned how JavaScript can modify the DOM by creating, removing, or changing elements.

## what i did
- I generated a simple **todo app** using AI and added it to my repository as `todo.html`.
- I reviewed the JavaScript code to understand how the app adds and removes tasks.
- I identified where the script selects elements, listens for events, and updates the DOM.
- I modified parts of the code to personalize the app and tested it in the browser.