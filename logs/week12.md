## session 21

## what i learned
- learned how APIs work and how they allow different applications to communicate by sending requests and receiving data (usually in JSON format)
- learned how to use `fetch()` in JavaScript to get data from an API and convert it into usable information
- understood both `.then()` and `async/await` methods for handling API responses
- reviewed how JavaScript can be used for animations using `setTimeout` and `setInterval`
- went over a full course recap including HTML structure, CSS styling, JavaScript basics, DOM manipulation, forms, arrays, loops, and debugging

## what i did
- reviewed all major topics from the course to prepare for upcoming assignments and projects
- practiced using APIs by looking at examples like the random joke API and people in space API
- worked through examples using `fetch()` to request and display live data
- explored both `.then()` and `async/await` approaches in code
- looked at simple JavaScript animation techniques using timing functions